
Options -MultiViews
RewriteEngine On

# For all other requests (except existing files), redirect to index.html for React routing
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [QSA,L]
